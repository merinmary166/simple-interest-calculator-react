1. index.html -> bootstrap
2. index.csss ->delete all and add margin,padding,
3. App.css  -> delete all
4. App.jsx  -> remove all except body,add h3, then style to div,then p tag,then div for rs 1500,total simple interest
5. In App.jsx, import material ui in other cmd and import it in app.jsx, then in form, copy the textfield code and give style 
6. Copy 3 times for 3 inputs.
7. Then for buttons, search stack (inorder to get in row) and button. Apply styles.
8. Then create 4 state
9. Validation for principal amount and then for others.
10. given an event onChange in principal amount., validate fn is written.then destructring. name is given to 3 i/p fields.
11. then reg expresn match(). if name = principal,setvalue otherwise give alert invalid input.
12. A div is created in return after principle amount for invalid input. Then state is created, const invalidPrinciple 
13. alert is removed, then set invalidPrinciple is set True and the div part in invalid input is cut and put curly braces and use truthy operator. then if the condition is
    not matching, then it set false. Done it for other rate and year. Target is also done.
14. Then if all are giving invalid input, calculate button must be disabled.
15. Then to calculate interest, on clicking write another function handleClick.write the fn for handleClick.
16. then inorder to prevent refreshing , use an html property called preventDefault.
17. const handReset fn 
18. then in textfield value = principle or nothing is given (i.e, if value is there, then principal amount is shown,otherwise show the placeholder so that's why null) and same applied to other two.
19. Then in h1 interest value is given.